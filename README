For this moment crawler do not download web sites, it is in intensive development.

To compile it run make
To start crawler run start

Stopping crawler is possible by killing process with id, you can find with ps -ax | grep crawler_system
